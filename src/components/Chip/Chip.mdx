---
name: Chip
menu: Components
---

import { Playground, Props } from 'docz'
import UseCaseInline from '../../docz-components/UseCaseInline/UseCaseInline';
import {Chip} from './Chip'

# Chip

Chip component allows the user to know information about some selection in a filtered system.

It also allows the user to remove the selection by clicking on the ```X``` icon.

## Props
<Props of={Chip} />

## Use Cases
<UseCaseInline
  title="Default"
  Component={
    <Chip label="Element" onClose={() => alert('clicked')} />
  }
  code={<code>{'<Chip label="Element" onClose={()=>{}} />'}</code>}
  description=""
/>
<UseCaseInline
  title="Fixed"
  Component={
    <Chip label="Element"/>
  }
  code={<code>{'<Chip label="Element" />'}</code>}
  description="Cannot be closed"
/>
<UseCaseInline
  title="Custom title"
  Component={
    <Chip label="KWC" title="Konstellation Web Components"/>
  }
  code={<code>{'<Chip label="KWC" title="Konstellation Web Components"/>'}</code>}
  description="Typically used when label is shortened"
/>

## Playground

<Playground>
  <Chip
    label="CLICK ME"
    title="Chip title!"
    onClose={() => alert('clicked')}
  />
</Playground>
