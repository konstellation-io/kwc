---
name: HorizontalBar
route: /Components/Layout/HorizontalBar
menu: Layout
---

import { Playground, Props } from 'docz'
import {HorizontalBar} from './HorizontalBar'
import {Button} from '../../Button/Button'

# HorizontalBar

HorizontalBar component align its content (children) an horizontal layout, inserting 
elements starting from right to left.

This component is used for bottom bars that contain action Buttons.

## Props
<Props of={HorizontalBar} />

## Playground

<Playground>
  <HorizontalBar>
    <Button label="ACTION" height={30} primary />
    <Button label="CANCEL" height={30} />
  </HorizontalBar>
</Playground>
